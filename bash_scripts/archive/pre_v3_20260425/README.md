# Pre-v3 BayesEoR Sweep Wrapper Archive

This directory snapshots the Airy and Gaussian BayesEoR sweep wrapper scripts
before the v3 wrappers were added on 2026-04-25.

The active v3 wrappers live in `bash_scripts/` and explicitly set the clean v2
results root plus the appropriate `data.named_roots` key.
